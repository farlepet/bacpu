ADD instruction
---------------

	Adds one register to another register, and stores the value in the first
register.

*** Instruction Flow ***
ADD
    * Check that both registers are valid
        * False
            * Increase PC
            * Call exception
        * True
            * Set ALU to addition
            * Add two values
            * Set first register to the result from the ALU
            * Set Flags
                * ALU carry == 1 -> Flags.Carry
            * Increase PC

*** Instruction Format ***

	Instruction Format: OpCode with two registers
	First Register:     Augend and Sum
	Second Register:	Addend
